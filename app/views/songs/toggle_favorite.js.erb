var songId = '<%= @song.id%>'

function toggleLikeButton(songId, innerHTML) {
  let likeButton = document.querySelector(`.like-icon[data-id='${songId}']`)
  likeButton.classList.toggle('red');
}
toggleLikeButton(songId);
